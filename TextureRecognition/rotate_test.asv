%% Init MATLAB
close all;
clear all;
clc;
%%
% h = surf(peaks(20));

% x = -pi:0.1:pi;
% y = sin(x);
% h = plot(x,y);

h = rectangle('Position',[1,1,w00,h00],'EdgeColor','r','Curvature',[0,0]);

for a=0:360
    rotate(h,[0 0 1],a);
%     drawnow;
    pause(0.1);
end;
%% EOF